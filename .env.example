# Upstash Redis (free tier) - powers short card links (api/cards.ts).
# Create a database at https://upstash.com, then copy its REST URL/token here
# and into the Vercel project's Environment Variables settings.
UPSTASH_REDIS_REST_URL=
UPSTASH_REDIS_REST_TOKEN=
